{
  "name": "rythmic",
  "version": "0.1.0",
  "description": "A rhythm roguelike. The enemy sets the tempo; you answer with your mouse.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "balance": "python3 tools/balance_sim.py",
    "test": "node tests/combat.test.js",
    "test:scene": "node tests/scene.test.mjs",
    "test:browser": "vite build && node tests/smoke.mjs dist"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "puppeteer": "^25.4.0",
    "vite": "^6.0.0"
  },
  "//optional": "puppeteer powers `npm run test:browser`. It is not a devDependency because it downloads a private ~150MB Chrome. Install it with `npm i -D puppeteer` if you want the full browser click-through; the script skips cleanly when it is absent."
}
